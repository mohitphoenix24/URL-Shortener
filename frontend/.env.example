# Copy to .env — not secret, just kept out of git so each environment (local
# dev, later a deployed build) can point at a different backend without a code change.
VITE_API_BASE_URL=http://localhost:4500
